<Window x:Class="EjemploEEF1.View.AlumnoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EjemploEEF1.View"
        mc:Ignorable="d"
        Title="{Binding Titulo}" Height="600" Width="800">
    <Grid>
        <DataGrid x:Name="dataGrid" ItemsSource="{Binding ListaAlumnos}" AutoGenerateColumns="False" Margin="10" RenderTransformOrigin="0.5,0.5">
            <DataGrid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </DataGrid.RenderTransform>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Alumno Id" Binding="{Binding AlumnoId}"/>
                <DataGridTextColumn Header="Carné" Binding="{Binding Carne}"/>
                <DataGridTextColumn Header="Nombres" Binding="{Binding Nombres}"/>
                <DataGridTextColumn Header="Apellidos" Binding="{Binding Apellidos}"/>
                <DataGridTextColumn Header="Carrera" Binding="{Binding Carrera.Descripcion}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
